{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run user service",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/apps/user-service/cmd",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Run message service",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/apps/message-service/cmd",
      "cwd": "${workspaceFolder}"
    }
  ],
  "compounds": [
    {
      "name": "Run user + message services",
      "configurations": ["Run user service", "Run message service"],
      "stopAll": true
    }
  ]
}
