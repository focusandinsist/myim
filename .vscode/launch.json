{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run myim",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/app/cmd",
      "cwd": "${workspaceFolder}"
    }
  ]
}
